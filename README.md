# Today We Learn Together

Static GitHub Pages site. Open `index.html` directly or serve this folder with any static file server.

The model comparisons page fetches the existing published Google Sheets CSV feeds at runtime. The rest of the site has no external runtime dependencies.

